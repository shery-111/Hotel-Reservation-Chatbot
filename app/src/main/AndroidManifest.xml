<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hotelreservationchatbot">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HotelReservationChatbot">
        <activity android:name=".HotelInfo"></activity>
        <activity android:name=".AccountInfo" />
        <activity android:name=".Chatbot" />
        <activity android:name=".Forgot" />
        <activity android:name=".Register" />
        <activity android:name=".Login" />
        <activity android:name=".who_r_u" />
        <activity android:name=".HotelStaff" android:theme="@style/Theme.HotelReservationChatbot..NoActionBar"
            android:exported="true"/>
        <activity android:name=".Traveler" android:theme="@style/Theme.HotelReservationChatbot..NoActionBar"
            android:exported="true"/>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>